<div class="row">
 <%= form_for @asiento do |f| %>
 <div>

      <%= f.fields_for(:asiento) do |asiento_f| %>
        <% render partial: 'proforma', :locals => {:asiento_f => asiento_f, :clase => "asiento"} %>
      <% end %>

    <div class="col-md-1 col-md-offset-7">
      <div class="form-group">
        <%= link_to 'Regresar', asientos_path %>
      </div>
    </div>
    <div class="col-md-4">
      <div class="form-group">
        <button id = "anatext_submit_button", onclick="toggle_spinner(this);void(0)", class="btn btn-lg">
          <span class="glyphicon"></span> Generar trámite</button>
        </button>
        <button id = "spinner", style="display:none", class="btn btn-lg btn-warning">
          <span class="glyphicon glyphicon-refresh glyphicon-refresh-animate"></span> Generando trámite ...</button>
        </button>
      </div>
    </div>

   </div>
  <% end %>
</div>

